import { CommaFloatingPointPipe } from './comma-floating-point.pipe';

describe('CommaFloatingPointPipe', () => {
  it('create an instance', () => {
    const pipe = new CommaFloatingPointPipe();
    expect(pipe).toBeTruthy();
  });
});
